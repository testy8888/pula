pipeline {
    agent {label 'jwork'}

    stages {
        stage('test') {
            steps {
                sh 'sudo cat /var/log/messages'
                sh 'sh /tmp/jenki.sh'
                sh '/tmp/test'
            }
        }
        stage('test4') {
            steps {
                emailext(body: 'yyyyyyyyyyyy', subject: 'test', to: 'lelunicu@gmail.com')
            }
     }
}
    post {
        always {
            emailext body: 'A Test EMail', recipientProviders: [[$class: 'DevelopersRecipientProvider'], [$class: 'RequesterRecipientProvider']], subject: 'Test'
        }
    }
        
}
   
