pipeline {
    agent {label 'jwork'}

    stages {
        stage('test') {
            steps {
                sh 'sudo cat /var/log/messages'
                sh 'sh /tmp/jenki.sh'
                sh '/tmp/test'
            }
        }
        stage('test4') {
            steps {
                echo 'test'
            }
     }
}
    post {
        always {
           emailext(body: 'yyyyyyyyyyyy', subject: 'test', to: 'linuly@mail.com')
        }
    }
        
}
   
