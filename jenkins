pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                sh 'sudo cat /var/log/messages'
                sh 'sh -c /root/jenki.sh'
            }
        }
        stage('test4') {
            steps {
                echo 'y'
            }
    }
}
}
   
